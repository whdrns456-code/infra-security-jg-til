# 📅 TIL: 2026-02-21

## 📝 1. 오늘의 학습 주제 및 핵심 내용
- **주요 주제:** 엔터프라이즈 리눅스 스토리지(LVM) 최적화 및 시스템 보안(SSH Hardening)
- **핵심 개념 1: LVM(Logical Volume Manager)** - 물리 디스크를 논리적 계층으로 분리하여 가동 중에도 용량 확장/축소가 가능한 유연한 스토리지 구조 이해
- **핵심 개념 2: UUID 기반 Mount 전략** - 장치명(`/dev/sdb`)이 변해도 시스템 부팅 안정성을 보장하는 고유 식별자(UUID) 활용법
- **핵심 개념 3: SSH Hardening** - 루트 접속 차단, 포트 변경, 패스워드 인증 비활성화를 통한 서버 접근 보안 강화

---

## 💻 2. 실습 및 구현 내용
### ✅ 실습 목표
- LVM을 활용한 가변 스왑 공간 및 데이터 볼륨 구축
- SSH 보안 강화를 통한 비정상 접근 원천 차단

### 🛠️ 구현 결과

```bash
# [LVM 구성] 물리 볼륨 -> 볼륨 그룹 -> 논리 볼륨 생성
pvcreate /dev/nvme0n2
vgcreate vg_data /dev/nvme0n2
lvcreate -L 4G -n lv_swap vg_data
lvcreate -l 100%FREE -n lv_work vg_data

# [스왑 및 파일시스템 활성화]
mkswap /dev/vg_data/lv_swap
mkfs.xfs /dev/vg_data/lv_work

# [UUID 기반 마운트 설정] /etc/fstab
# blkid로 확인한 UUID를 사용하여 부팅 시 자동 마운트
# UUID=xxxx-xxxx  swap  swap  defaults  0 0
# UUID=yyyy-yyyy  /mnt/work xfs defaults 0 0

# [SSH 보안 강화] /etc/ssh/sshd_config
sed -i 's/#Port 22/Port 2022/' /etc/ssh/sshd_config
sed -i 's/#PermitRootLogin yes/PermitRootLogin no/' /etc/ssh/sshd_config
sed -i 's/PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config
systemctl restart sshd